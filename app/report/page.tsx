'use client'
import { useState } from 'react'
import Link from 'next/link'
import { ArrowLeft, Leaf, Send } from 'lucide-react'
export default function ReportPage() { const [sent, setSent] = useState(false); const [form, setForm] = useState({ locationName: '', lat: '', lng: '', threatType: 'Deforestation', description: '', photoUrl: '' }); const update = (key: string, value: string) => setForm(f => ({ ...f, [key]: value })); async function submit(e: React.FormEvent) { e.preventDefault(); if (!form.locationName || !form.description || !form.lat || !form.lng) return; await fetch('/api/reports', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(form) }); setSent(true) } return <main className="min-h-screen"><header className="border-b border-border px-5 py-4 md:px-10"><Link href="/" className="flex w-fit items-center gap-2.5"><span className="grid size-9 place-items-center rounded-xl bg-primary text-primary-foreground"><Leaf className="size-5" /></span><span className="font-semibold">GeoEcoz</span></Link></header><div className="mx-auto max-w-2xl px-5 py-12 md:py-20"><Link href="/" className="flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4" />Back to dashboard</Link><p className="mt-12 text-xs uppercase tracking-[0.2em] text-primary">Field observation</p><h1 className="mt-3 text-4xl font-medium tracking-tight">Add a signal from the ground.</h1><p className="mt-4 text-muted-foreground">Share an observation with the GeoEcoz monitoring network.</p>{sent ? <div className="mt-10 rounded-2xl border border-primary/40 bg-primary/10 p-6"><h2 className="text-xl font-medium">Observation received</h2><p className="mt-2 text-sm text-muted-foreground">Your field note has been added to the monitoring queue.</p><Link href="/" className="mt-6 inline-block text-sm text-primary">Return to dashboard →</Link></div> : <form onSubmit={submit} className="mt-10 flex flex-col gap-5 rounded-2xl border border-border bg-card p-6 md:p-8">{[['locationName','Location name','e.g. North Amazon corridor'],['lat','Latitude','Decimal degrees'],['lng','Longitude','Decimal degrees'],['photoUrl','Photo URL (optional)','Paste a secure image URL']].map(([key,label,placeholder]) => <label key={key} className="flex flex-col gap-2 text-sm"><span>{label}</span><input required={key !== 'photoUrl'} value={form[key as keyof typeof form]} onChange={e => update(key, e.target.value)} placeholder={placeholder} className="rounded-lg border border-border bg-background px-3 py-3 outline-none focus:ring-2 focus:ring-primary/50" /></label>)}<label className="flex flex-col gap-2 text-sm"><span>Threat type</span><select value={form.threatType} onChange={e => update('threatType', e.target.value)} className="rounded-lg border border-border bg-background px-3 py-3 outline-none"><option>Deforestation</option><option>Wildfire</option><option>Coral bleaching</option><option>Poaching</option></select></label><label className="flex flex-col gap-2 text-sm"><span>Observation</span><textarea required value={form.description} onChange={e => update('description', e.target.value)} placeholder="What did you observe? Include details that could help a field team respond." rows={5} className="resize-none rounded-lg border border-border bg-background px-3 py-3 leading-6 outline-none focus:ring-2 focus:ring-primary/50" /></label><button className="mt-2 flex items-center justify-center gap-2 rounded-lg bg-primary px-4 py-3 text-sm font-medium text-primary-foreground hover:opacity-90">Submit observation <Send className="size-4" /></button></form>}</div></main> }
